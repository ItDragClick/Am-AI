Act as a Principal Software Engineer and Expert Minecraft Fabric Mod Developer specializing in client-side
  automation for version 26.2. We are building a comprehensive, production-grade Client-Side Fabric Mod that provides
  an external desktop dashboard control panel (via Java Swing) alongside the running game client, connecting an
  in-game avatar to a local Gemma-2b LLM via Ollama and directing physics/movement using the Baritone API.

  Please implement this entire architecture in a modular, highly organized, and robust manner based on the following
  detailed specification:

  1. EXTERNAL UI & WINDOW MANAGEMENT (Java Swing)
  - Create a class 'AIDashboardFrame' extending JFrame, which must run on a completely separate thread using
  'SwingUtilities.invokeLater' during mod initialization ('ClientModInitializer') to ensure the Minecraft render
  thread never blocks.
  - The UI window must implement a BorderBagLayout or GridBagLayout containing three key panels:
    * Settings Panel (Top): JTextField for 'Ollama Endpoint URL', JTextField for 'Target Model ID', JTextField for 'AI
  Command Prefix Trigger' (defaulting to "!ai"), a 'Save Config' JButton, a 'Test Connection' JButton, and a visual
  status indicator JRadioButton (showing Active/Inactive states).
    * System Console Log Panel (Center): A large, non-editable JTextArea embedded within a JScrollPane, featuring a
  thread-safe 'public static void appendSystemLog(String message)' method capable of updating the window from any
  thread inside the mod environment without concurrency failures.
    * Manual Prompt Panel (Bottom): A JTextField input along with a 'Send' button allowing direct, manual prompting of
  the AI brain without needing to type inside the game client window.

  2. CONFIGURATION STORAGE & PERSISTENCE (GSON)
  - Create a serializable data class 'AIModSettings' containing fields for: the endpoint URL string, target model ID
  string, prefix key string, and a boolean flag for the active toggle state.
  - Implement a static helper utility 'SettingsPersistenceManager' that automatically saves and loads this
  configuration object to a local JSON file situated at '.minecraft/config/ai_companion_settings.json' using
  Minecraft's native GSON library.
  - Wire this manager directly to the Swing interface: populate the fields automatically upon window load, save the
  payload immediately upon clicking 'Save Config', and provide a functional 'Test Connection' routine that performs a
  quick, non-blocking HTTP GET request to check if the Ollama instance is alive.

  3. IN-GAME EVENT HOOKS & CHAT LISTENER
  - Implement a client-side network interceptor hook (via Mixin targeting 'ClientPlayNetworkHandler' or standard
  Fabric client event registries) to scan all incoming server-wide chat messages.
  - If the inbound chat text begins with the user-defined prefix string (e.g. "!ai"):
    * Extract the sender's username string and the core prompt message text.
    * Check our configuration profile: if the system is globally toggled off or if the sender's name matches our own
  local player's name (to avoid infinite reflection loops), discard the event.
    * If valid, instantly push a clean notification update to the external Swing JTextArea console logging the inbound
  task request.

  4. ASYNCHRONOUS LLM CONNECTION LOOP (Ollama Engine)
  - Build an asynchronous network handler module ('OllamaNetworkClient') that creates an HTTP POST request targeting
  the configured Ollama URI (e.g., 'http://localhost:11434/api/generate').
  - The request payload must use the user-defined model ID and pass a rigid System Context Prompt framing Gemma-2b's
  personality: "You are an autonomous AI companion playing Minecraft version 26.2 with access to the Baritone API. You
  must interpret incoming instructions and output your reply strictly in raw, unquoted valid JSON layout containing
  exactly two keys: 'chat' (a short text under 100 characters to speak in server chat) and 'action' (the command
  string to direct your body, e.g. 'goto X Y Z', 'mine block_name', or 'stop'). Output no additional sentences."
  - Parse the resulting string stream safely. If a malformed response occurs, gracefully fall back to a safe JSON
  default: {"chat": "My thoughts got jumbled!", "action": "stop"}.

  5. THREAD-SAFE BARITONE API & ACTION EXECUTION BRIDGE
  - Because the network client resolves requests on a background worker thread, you must wrap all subsequent
  interactions with the Minecraft client environment inside a thread-safe task scheduler:
  'MinecraftClient.getInstance().execute(() -> { ... });'. This prevents fatal client-side race conditions.
  - Implement an engine mapper class 'AIActionBridge' to parse the 'action' and 'chat' keys from the JSON response:
    * For the 'chat' value: Call 'MinecraftClient.getInstance().player.networkHandler.sendChatMessage(chatText)' to
  cast the text into the game world.
    * For the 'action' value string:
      - If it contains 'goto X Y Z': Parse the numerical coordinates, instantiate a Baritone 'GoalXZ' or 'GoalBlock',
  and dispatch it through the primary process system using
  'BaritoneAPI.getProvider().getPrimaryBaritone().getCustomGoalProcess().setGoalAndPath(goal)'.
      - If it contains 'mine block_name': Convert the string to a valid block identity and assign a Baritone
  'MineProcess' task.
      - If it contains 'stop': Call Baritone's primary pathfinder clearing routine to instantly freeze the player
  safely.

  Generate the full, complete class files, gradle dependencies framework, and cross-thread callbacks required to form
  a seamless client mod environment. Do not use incomplete methods or logic stubs.
  file are in C:\Users\sarut\workspaces\amai
  you can read Claude.md for more info then if done anything try update it.